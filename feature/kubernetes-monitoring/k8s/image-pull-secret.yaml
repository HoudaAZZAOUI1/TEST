apiVersion: v1
kind: Secret
metadata:
  name: ghcr-secret
  namespace: ecommerce-recommendation
type: kubernetes.io/dockerconfigjson
data:
  # Generate with: kubectl create secret docker-registry ghcr-secret \
  #   --docker-server=ghcr.io \
  #   --docker-username=YOUR_USERNAME \
  #   --docker-password=YOUR_PAT \
  #   --namespace=ecommerce-recommendation \
  #   --dry-run=client -o yaml | grep .dockerconfigjson | awk '{print $2}' | base64 -d
  .dockerconfigjson: |
    # Replace this with your actual docker config JSON
    # Or create the secret using: kubectl create secret docker-registry ghcr-secret \
    #   --docker-server=ghcr.io \
    #   --docker-username=YOUR_USERNAME \
    #   --docker-password=YOUR_PAT \
    #   --namespace=ecommerce-recommendation

